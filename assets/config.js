/* ============================================================
   AKSHAT CHEMICALS — site configuration
   GAS_URL: paste your Google Apps Script Web App URL here after
   following GOOGLE-SETUP.md. While empty, enquiries fall back to
   FormSubmit email + local log so the site never loses a lead.
   ============================================================ */
const CONFIG = {
  GAS_URL: "",            // e.g. "https://script.google.com/macros/s/AKfycb.../exec"
  ADMIN_KEY: "akshat2007" // must match SECRET_KEY in Code.gs; also the admin passcode
};
